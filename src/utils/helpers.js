export const getPublicUrl = () => {
    return "http://localhost:3000/";
}

export const getSocialIcon = (fileName) => {
    return "/socialIcons/"+fileName;
}

export const getImage = (fileName) =>{
    return "/image/"+fileName;
}
